import torch.optim as optim


config = {}
config['data_dir'] = 'datasets/'
config['dataset'] = '1000'
config['train'] = True

# TODO: Populate the config flags needed for hyperparameters and training
